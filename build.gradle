task wrapper(type: Wrapper) {
    gradleVersion = '4.6'
}

allprojects {
    buildscript {
        repositories {
            mavenCentral()
        }
        dependencies {
            classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlin_version}"
        }
    }

    repositories {
        mavenCentral()
    }
}